services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: dynamic_form_lottery
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
